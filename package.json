{
  "name": "vitest-pool-workers-error-project",
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "deploy": "wrangler deploy --minify src/index.ts",
    "test": "vitest --run"
  },
  "dependencies": {
    "hono": "^4.5.1"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.4.14",
    "@cloudflare/workers-types": "^4.20240403.0",
    "miniflare": "3.20240610.1",
    "vitest": "1.5.0",
    "wrangler": "^3.56.0"
  }
}